# Regression_model
try the regression model with streamlit app.
training my model with a new dataset that come as input from the user and get the records of this training and shows it,
then make the user input a random sample to test this model.
